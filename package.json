{
  "name": "uniswap-v2-subgraph",
  "version": "1.0.0",
  "repository": "https://github.com/graphprotocol/uniswap-v2-subgraph",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "create-local": "graph create gop_subgraph --node $GRAPH_NODE_ADDRESS",
    "deploy-local": "graph deploy gop_subgraph --debug --ipfs $IPFS_ADDRESS --node $GRAPH_NODE_ADDRESS",
    "watch-local": "graph deploy graphprotocol/Uniswap2 --watch --debug --node $GRAPH_NODE_ADDRESS --ipfs $IPFS_ADDRESS",
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.20.0",
    "@graphprotocol/graph-ts": "^0.20.0",
    "@typescript-eslint/eslint-plugin": "^2.0.0",
    "@typescript-eslint/parser": "^2.0.0",
    "eslint": "^6.2.2",
    "eslint-config-prettier": "^6.1.0",
    "prettier": "^1.18.2",
    "typescript": "^3.5.2"
  }
}
